<main>
  <div class="top-main">
    <div class="top-heading">
    <h1>Tweet App</h1>
    </div>
    <div class="top-sign">
       <% if user_signed_in? %>
       <span><%= link_to "Tweetes",  posts_path %></span>
       <% else %>
   <span><%= link_to "Sign in", new_user_session_path %></span>
   <span><%= link_to "Sign Up",  new_user_registration_path %></span>
   <% end %>
    </div>
  </div>
</main>
